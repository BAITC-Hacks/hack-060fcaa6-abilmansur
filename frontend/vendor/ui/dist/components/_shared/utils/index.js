export const isChatEmpty = ({ isLoadingMessages, messages, }) => {
    return !isLoadingMessages && messages.length === 0;
};
export { safeOpenUrl, safeUrl, toCssUrl } from "./safeUrl";
//# sourceMappingURL=index.js.map