# Export these variables in your shell, or use uv run --env-file .env ...
MONEY_GRAPH_AI_ENABLED=0
# Optional: model available to your Codex account; omit to use Codex default.
# MONEY_GRAPH_CODEX_MODEL=
# MONEY_GRAPH_DATA_DIR=/absolute/path/to/data
MONEY_GRAPH_CORS_ORIGINS=http://localhost:3000,http://localhost:5173
